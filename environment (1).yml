name: moral-values-efl
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  # --- Core scientific stack ---
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - statsmodels>=0.14
  # --- Visualization ---
  - matplotlib>=3.7
  - seaborn>=0.12
  # --- Notebook environment ---
  - jupyter
  - jupyterlab
  - notebook
  # --- Document & spreadsheet I/O ---
  - openpyxl>=3.1
  - pip:
      - python-docx>=1.1
      - pyyaml
